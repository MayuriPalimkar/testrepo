package interface1;

public interface interfaace {
  
	int no = 100;  //variables declared inside the interface are by default static and final
	
	void interface1();
	
	void interface2();
	
	void interface3();
	 //methods declared in the interface are by default public and abstract
	
	public abstract void interface4();
	
	//interface1 I1 = new interface1();
	//cannot instantiate the type interface1
	//cannot create object inside the interface
	
}
